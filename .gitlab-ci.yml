stages:
  - composer
  - list

composer:
  stage: composer
  image: composer:latest
  script:
    - composer install
  artifacts:
    paths:
      - vendor/

list:
  stage: list
  script:
    - ls -lah
