stages:
  - composer
  - list

composer:
  stage: composer
  image: composer:latest
  script:
    - cd app && composer install
  artifacts:
    paths:
      - app/vendor/

list:
  stage: list
  script:
    - ls -lah
